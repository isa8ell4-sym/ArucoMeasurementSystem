from setuptools import setup, find_packages

setup(
    name="calibration",               # Name of your package
    version="0.1",
    packages=find_packages(),
    description="Calibration module for camera processing",
    author="Isabella Rosenstein",
    install_requires=[]  # Add dependencies if needed
)